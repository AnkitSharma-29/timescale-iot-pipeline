apiVersion: 1

datasources:
  - name: TimescaleDB
    type: postgres
    access: proxy
    url: timescaledb:5432
    user: iot
    jsonData:
      database: iot
      sslmode: disable
      postgresVersion: 1600
      timescaledb: true
    secureJsonData:
      password: iotpass
    isDefault: true
